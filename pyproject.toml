[project]
name = "goleta-ai-exam"
version = "0.1.0"
description = "This project is a minimal task queue that is implemented in python."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pydantic-settings>=2.15.0",
]
